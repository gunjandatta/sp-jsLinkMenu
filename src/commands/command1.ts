/**
 * The click event for 'Command 1'
 * @param items - The selected list items.
 */
export const Command1 = (items: Array<any>) => {
    // Alert how many items are selected
    alert("Number of items selected: " + items.length);
}